fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
httpx>=0.28.0
structlog>=24.4.0
orjson>=3.10.0
aiofiles>=24.1.0
python-multipart>=0.0.19

# Databricks SQL connector
databricks-sql-connector>=3.4.0

# Caching (optional — app works without)
upstash-redis>=1.3.0

# Pandas for data manipulation
pandas>=2.2.0

# LangGraph / LangChain
langchain>=0.3.0
langgraph>=0.2.0
langchain-core>=0.3.0

# FAISS (optional — semantic search disabled if not installed)
# faiss-cpu is not yet available for Python 3.14
# Install manually if using Python 3.11/3.12:
#   pip install faiss-cpu
# For now, FAISS is optional and the app degrades gracefully without it.

# NumPy (for FAISS operations)
numpy>=1.26.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Rate limiting
slowapi>=0.1.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
